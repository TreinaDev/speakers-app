<turbo-stream action="replace" target="ongoing_events_list">
  <template>
    <%= render 'events/event_list', event_list: @ongoing_events, paginated_events: @paginated_ongoing_events, param_name: 'ongoing_page' %>
  </template>
</turbo-stream>

<turbo-stream action="replace" target="future_events_list">
  <template>
    <%= render 'events/event_list', event_list: @future_events, paginated_events: @paginated_future_events, param_name: 'future_page' %>
  </template>
</turbo-stream>

<turbo-stream action="replace" target="past_events_list">
  <template>
    <%= render 'events/event_list', event_list: @past_events, paginated_events: @paginated_past_events, param_name: 'past_page' %>
  </template>
</turbo-stream>
